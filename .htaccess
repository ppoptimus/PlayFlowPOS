<IfModule mod_rewrite.c>
    RewriteEngine On

    # Keep direct /public requests unchanged.
    RewriteRule ^public/ - [L]

    # Route every request through Laravel's public directory.
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
